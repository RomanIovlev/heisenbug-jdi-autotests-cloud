# Jdi properties
driver=chrome
site.url=http://autotests.cloud:3000/

# Selenoid properties
driver.remote.run=false
remote.type=selenoid

# Common properties
buildNumber=${build_number}
jobBaseName=${job_base_name}
webMobileDevice=${web_mobile_device}
remoteDriverUrl=${remote_driver_url}
videoStorageUrl=${video_storage_url}
